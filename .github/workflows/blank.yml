name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Gradle Android
    - uses: Raul6469/android-gradle-action@2.0.0
    - with:
        script: ./gradlew assemble.
        echo:  "execute asseble"
